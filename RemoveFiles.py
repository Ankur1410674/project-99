import os
import shutil
path = input("enter the name of the directory to be sauted")
os.path.exists(path) 
for file in listoffiles:
    name,ext = os.path.splitext(file)
    ext = ext[1:]
    if ext == " ":
        continue
    if ctime = os.stat(path).st_ctime
        return ctime
  